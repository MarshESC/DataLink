# dependencies
node_modules/

# env / secrets
.env
.env.*
!.env.example
*.pem
*.key
# exception: AWS RDS's public CA bundle — no secret, needed at runtime for TLS cert verification
!app/global-bundle.pem

# build artifacts
dist/
build/

# editor / tool local state
.vscode/
.cursor/
.impeccable/
graphify-out/

# OS
.DS_Store

# logs
*.log
npm-debug.log*
